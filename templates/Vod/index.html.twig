{% extends 'base.html.twig' %}

{% block title %}Accueil{% endblock %}

{% block body %}
	{% include('vod/partials/header.html.twig') %}
  <div class="container">
    <h1>Bienvenue sur notre site de streaming / VOD</h1>
    <div class="btn-group">
      <a href="{{ path('films_index') }}" class="btn btn-primary">Films</a>
      <a href="#" class="btn btn-primary">Séries</a>
    </div>
  </div>
{% endblock %}

{% block stylesheets %}
  <style>
    .container {
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      height: 100vh;
    }

    .btn-group {
      margin-top: 20px;
    }

    .btn {
      margin-right: 10px;
    }
  </style>
{% endblock %}